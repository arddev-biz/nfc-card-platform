# NFC Card Platform

Phase 1 — Project Foundation.

See the implementation summary provided alongside this project for full
setup instructions (installing dependencies, configuring `.env`, running
the Prisma migration, and starting the dev server).
